debug: true

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file://home/bvpelt/Develop/demo-complaints-config/data
server:
  port: 8888
